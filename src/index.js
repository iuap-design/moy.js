
import createApp from './createApp'
import use from './use'
import createModel from './createModel'

/**
 * createApp() : 创建应用服务
 * use() : 使用 plugin 或 middleware
 * createModel() : 创建数据模型
 */
export default {
  createApp,
  use,
  createModel
}
